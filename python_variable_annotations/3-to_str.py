#!/usr/bin/env python3
"""function that takes a float as argument and returns the
string representation of the float"""


def to_str(n: float) -> str:
    """
    Convert a float to its string representation

    Args:
        n (float): The float to be converted to a string
        """
    return str(n)
